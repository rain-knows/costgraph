# Upstream provenance

The files in this directory, except `LICENSE` and this provenance note, are an
unchanged copy of the complete `design-systems/ibm/` subtree from OpenDesign.

| Field | Value |
| --- | --- |
| Repository | `https://github.com/nexu-io/open-design` |
| Commit | `c5ae6292c464094de80d0191fab50144bff8a027` |
| Source path | `design-systems/ibm/` |
| Retrieved | `2026-09-07` |
| Upstream file count | `41` |
| License | Apache License 2.0; see `LICENSE` |

The package is a curated OpenDesign fixture inspired by IBM and Carbon design
language. It is not an IBM product, endorsement, certification, or trademark
license. CostGraph-specific decisions live in the repository root `DESIGN.md`;
do not edit the copied package to encode product overrides.

The local byte-level SHA-256 manifest for the 41 copied upstream files is
[`UPSTREAM-SHA256.txt`](UPSTREAM-SHA256.txt). The repository documentation check
verifies every listed path and digest.

To refresh the package, retrieve a reviewed immutable commit, replace the
complete subtree, restore this provenance note and the matching upstream
license, then run `scripts/check_docs_sync.ps1` and the frontend visual checks.
