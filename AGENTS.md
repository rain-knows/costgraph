# CostGraph Agent 协作规则

本文件是仓库级规则入口。系统架构、数据、接口、运行方式和设计约束分别以
[`docs/README.md`](docs/README.md) 与 [`DESIGN.md`](DESIGN.md) 为入口；聊天记录、
截图和迁移源仓库不能替代当前代码、迁移、测试和运行结果。

## 实现原则

- 不以维护向后兼容性为目标。已经废弃的字段、路由、配置、存储适配器和代码路径直接移除，不增加兼容层、回退或数据迁移方案。
- 在完整满足当前需求的前提下选择最简单实现，不增加没有现实需求的抽象、配置和间接层。
- 按可端到端运行的最小版本渐进构建；稳定后再扩生产批次、BOM、异常和审批，不用未成熟复杂性替换可用产品。
- 保持模块化和清晰职责边界；前端负责交互呈现，API 负责传输与校验，领域/服务负责确定性规则，Repository 负责数据边界，Runtime 负责执行生命周期。
- 通用能力优先使用成熟且维护良好的库。新增依赖或自行实现前，必须先检查项目现有依赖的文档、类型定义和实际能力。
- 架构决策面向长期演进，不采用已知需要立即替换的权宜实现；设计前先研究成熟产品的模式和约定。

## 开工与事实边界

1. 先读 [`docs/README.md`](docs/README.md)，再按问题读取最小必要的原子文档和其代码锚点。
2. 修改前检查当前工作树，保留用户和其他 Agent 的并行改动；不得批量回滚或覆盖无关文件。
3. 金额、数量、舍入、权限、数据范围、状态机和报表结构由服务端确定性代码或数据库约束负责，模型输出不能成为事实或安全边界。
4. 正式运行面固定为 PostgreSQL、独立 Worker 和一套 LangGraph 外循环。样例 JSON 只进入导入流程，不是应用运行时 Repository。
5. 运行数据和产出始终按 `tenant_id + principal_id` 隔离；成本事实还必须来自当前已发布快照并满足执行上下文的数据范围。
6. Alembic 是数据库结构的唯一入口；禁止运行时自动建表或手工修改正式库后不补迁移。
7. UI 遵守 [`DESIGN.md`](DESIGN.md)，不得以占位数据或演示状态伪装未实现能力。

## 文档强制同步

- 新增或修改模块边界、Runtime/Harness、Tool/Provider、Event/Trace/Replay、表字段、接口、配置、状态、权限或业务规则时，必须同步 `docs/README.md` 指向的对应原子文档和验证用例。
- 修改 `AGENTS.md` 时，同一变更必须更新 `docs/README.md` 或 `docs/governance/documentation.md`；设计规则变更还必须更新 `DESIGN.md`。
- 当前事实与计划分开维护。事实必须能回指当前代码、单一 baseline 迁移、测试或运行证据；未实现阶段只能写入 `docs/plans/`。
- 删除实现时同时删除失效文档，不保留“仅供兼容”的说明。信息仍无法确认时明确记录未知并对齐 owner，不得猜测。

提交或交付前运行：

```powershell
.\scripts\check_docs_sync.ps1
git diff --check
```

## 验证与交付

- 验证范围与风险相称。数据库、权限、Agent 状态、SSE 和 API 变更必须覆盖对应自动化测试。
- 本地 fixture、离线 Eval、PostgreSQL 集成和真实 DeepSeek 验证分别记录，不得用一种结果代替另一种。
- 不提交密钥、数据库密码、日志、运行产物或迁移源历史数据。交付说明列出改动、依据、实际命令和未验证项。
